# 루월당 홈페이지 버전

기존 NewKind-Zheila 자료집을 루월당 단일 홈페이지 형태로 바꾼 예시입니다.

## 적용 방법
1. 기존 저장소에서 `index.html`을 이 파일로 교체합니다.
2. 사이드 페이지를 없애려면 다른 `.html` 파일은 삭제하거나 링크하지 않으면 됩니다.
3. 접속 코드는 `index.html` 하단의 `if(code === 'luwoldang')` 부분에서 원하는 코드로 변경하세요.
